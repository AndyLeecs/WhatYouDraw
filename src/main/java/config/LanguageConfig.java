package config;

import java.util.Locale;

/**
 * Language in this project, change the language in package @config and all the fxml files if this default configuration is changed
 */
public class LanguageConfig {
    public static final Locale LOCALE = Locale.ENGLISH;
}
